import { styled } from "styled-components";

export const Hero = styled.h1`
  margin: 56px auto;
  color: var(--Primary20);
  text-align: center;
`;

export const List = styled.ul`
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  margin-bottom: 100px;
`;
